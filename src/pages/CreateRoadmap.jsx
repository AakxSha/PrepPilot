<div className="roadmap-container">

  <div className="card">
    {/* existing form content stays same */}
  </div>

  {showRoadmap && (
    <div className="card roadmap">
      {/* existing roadmap output stays same */}
    </div>
  )}

</div>
